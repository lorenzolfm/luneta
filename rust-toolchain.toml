# The same pin that zellij uses. The flake is the source of truth on NixOS. This file
# gives a machine with rustup the same toolchain and target.
[toolchain]
channel = "1.95.0"
targets = ["wasm32-wasip1"]
